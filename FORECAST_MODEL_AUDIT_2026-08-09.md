# CPI / PPI / PMI 预测模块复核记录（2026-08-09）

## 结论

原 `scripts/generate_forecasts.py` 生成的是简化线性近似，不能代表交接文档确认的模型，历史“每日预测”也不是可验证的历史 vintage。该实现已移入 `outputs/forecast-approximate-legacy/` 留档，不再作为正式预测入口。

当前正式入口按旧 WSL 项目锁定逻辑重新生成：

- CPI：食品与非食品双 SARIMAX；蔬菜、猪肉 L0；CRB L0；PMI L1；食品权重为过去 12 个月均值并滞后一期；精确乘法换算同比。
- PPI：SARIMAX(1,0,1)×(1,0,0,12)；南华工业品 L0/L1，布伦特、动力煤、螺纹钢、铜、CRB L0；精确乘法换算同比。
- PMI：新订单、生产两个分项用高频代理动量扩展窗回归；从业人员、配送时间、原材料库存取上月值；按官方权重合成，配送时间先取 `100-x`。
- 历史回测均为 expanding-window，每个月只用该月以前的目标历史；标准化参数也只用预测时点以前的数据。

## 已修正问题

1. 删除 CPI/PPI 同比直接线性回归的错误近似。
2. 删除 PMI 使用航班、动力煤、钢材库存等错误代理直接回归总指数的实现。
3. 修正 CPI CRB 被错误映射为南华农产品、PPI CRB 与南华工业品重复的问题。
4. 修正 2026 年 6 月 PPI 实际同比被写成 3.9；国家统计局实际值为 4.1。
5. 修正精确同比换算在闰年二月没有重新对齐月末、从而漏掉 2025-02 的错误。
6. 取消无法验证的 2024 年以来历史每日平滑预测；页面改为 2020 年至今实际值、2023 年至今月末无前视预测，以及单独标注的当前确认点预测。
7. 入模指标逐项展示真实用途、滞后、月度聚合方式、原始频率和单位；同一序列只展示一次。
8. 本地预览增加固定命令 `npm run preview`，地址为 `http://127.0.0.1:4173/`。

## 锁定结果

2023-01 至 2026-05：

| 指标 | RMSE | MAE |
|---|---:|---:|
| CPI 同比 | 0.223 | 0.174 |
| PPI 同比 | 0.290（锁定文档约 0.291） | 0.224 |
| PMI | 0.604 | 0.503 |

2026 年 7 月确认点预测保持不变：

| 指标 | 点预测 | 建议报数 |
|---|---:|---:|
| CPI 同比 | 1.047766% | 1.0% |
| PPI 同比 | 3.981354% | 4.0% |
| PMI | 48.98 | 49.0 |

## 验证结果

- 严格数据校验：111/111 指标，覆盖率 100%，0 错误、0 警告。
- Python 测试：25 项全部通过（原 20 项 + 5 项预测完整性测试）。
- 生产构建：`npm run build:github` 通过。
- 本地 HTTP：`/` 与 `/data/forecasts.json` 均返回 200。

## 尚未满足的发布闸门

本机没有可用的 `IFIND_SKILL_DIR/call.py` 与 `mcp_config.json`，也没有 CPI/PPI/PMI 三条一致预期的固定 provider ID。因此页面不会把模型取整值伪装成 iFinD 一致预期。

`scripts/fetch_forecast_consensus.py` 现已改为强制要求：

- `IFIND_CPI_CONSENSUS_ID`
- `IFIND_PPI_CONSENSUS_ID`
- `IFIND_PMI_CONSENSUS_ID`

解析器会核对接口返回的 `index_id`，发现模糊匹配漂移就失败且不覆盖缓存。在这三条真实接口数据完成验收前，保持禁止发布。
