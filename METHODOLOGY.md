# 宏观脉搏：指标加工与合成方法

本文档与 `config/indicators.json`、`scripts/update_dashboard.py` 的实际计算逻辑对应。页面内点击任一指标名称，也可以查看该指标自己的公式、原始分项、权重和加工步骤。

## 通用数据处理

1. 将接口记录统一为 `date`、`value` 两列。
2. 无法解析日期或数值的记录被剔除；同一天重复记录保留最后一条。
3. 日频指标可按配置计算滚动 7 日合计、滚动 7 日均值；周频平滑指标可计算滚动 28 日均值。
4. 最新值与 7 日前最近一个有效值比较，得到周变化。
5. 合成指标中的分项短暂缺数时可以使用最近值，但最多向前填充 10 天。

## 标准化合成框架

对第 i 个分项：

`z(i,t) = [x(i,t) - mean(x(i))] / std(x(i))`

合成指数：

`Index(t) = 100 + 10 × Σ[w(i) × z(i,t)] / Σw(i)`

因此，100 表示各分项处于各自历史平均附近；指数相对 100 每变动 10 点，约代表合成值变化 1 个历史标准差。均值和标准差使用当前数据文件覆盖的校准区间，并在每日更新时重估。

## 重点城市地铁客流指数

原始分项：

- 上海地铁客运量：`CJHX:METRO_SHANGHAI`，权重 1.0
- 广州地铁客运量：`CJHX:METRO_GUANGZHOU`，权重 1.0

公式：

`Metro(t) = 100 + 10 × [z(MA7(上海,t)) + z(MA7(广州,t))] / 2`

处理顺序：

1. 分别清洗两城每日客流数据。
2. 分别计算自然日滚动 7 日均值，以减弱工作日结构和节假日错位噪声。
3. 两个 7 日均值序列分别计算 z 分数。
4. 按日期对齐，等权平均，再映射为基准 100 的指数。

如需增加北京、深圳等城市，只需在该指标的 `series` 数组中增加代码和权重；公式的分母按有效权重之和自动调整。

## 轮胎开工综合指数

`Tire(t) = 100 + 10 × [z(半钢胎开工率,t) + z(全钢胎开工率,t)] / 2`

半钢胎和全钢胎等权。两者分别标准化后再合成，避免原始均值或波动率差异造成某一分项天然占优。

## 钢材社会库存综合指数

`SteelInv(t) = 100 + 10 × [1.2z(螺纹钢) + 1.0z(热卷) + 0.6z(冷轧) + 0.6z(中板)] / 3.4`

权重分别为 1.2、1.0、0.6、0.6，突出交易更活跃、对宏观需求更敏感的螺纹钢与热卷。

## 铜库存综合指数

`CopperInv(t) = 100 + 10 × [1.2z(上期所铜库存) + 0.8z(LME铜库存)] / 2.0`

境内库存权重 1.2，境外库存权重 0.8；两个市场先独立标准化，避免绝对库存规模差异主导合成结果。

## 从指标变化到债市信号

百分比变化指标：

`change(t) = [X(t) / X(t-7) - 1] × 100%`

百分点或水平变化指标：

`change(t) = X(t) - X(t-7)`

信号强度：

`score(t) = clip{[change(t) - mean(change)] / std(change) × 35 × bond_direction, -100, 100}`

- `bond_direction = 1`：指标上升通常利多债市，例如库存累积。
- `bond_direction = -1`：指标上升通常利空债市，例如需求或生产改善。
- 分值为正显示红色“利多”，分值为负显示绿色“利空”。
- 绝对值小于 15 归为中性，但趋势矩阵仍显示真实分值，不再留空。

## 配置一个新的合成指标

在 `config/indicators.json` 中为指标提供多个 `series`，并设置：

```json
{
  "aggregate": "standardized_mean",
  "component_preprocess": "rolling_7d_mean",
  "series": [
    {"code": "SOURCE:SERIES_A", "weight": 1.0},
    {"code": "SOURCE:SERIES_B", "weight": 0.8}
  ]
}
```

`component_preprocess` 可省略，或使用 `rolling_7d_mean`、`rolling_4w_mean`。建议同时填写 `methodology`，让页面直接展示业务名称、完整公式、校准窗口与加工步骤。
