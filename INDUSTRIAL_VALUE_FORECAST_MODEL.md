# 工业增加值底层统计桥模型

## 为什么推翻原模型

原生产模型直接用五个高频因子回归工业增加值同比。该做法忽略了同比增速中已经由前 11 个月产出和上年基数决定的部分，因此只能学到一个平滑的中心值。在 2023 年 3 月至 2026 年 7 月的走步样本中，原模型有以下问题：

- 预测月度变化的标准差只有实际值的 45%；
- 预测变化与实际变化的相关系数仅 0.038；
- 实际同比变动超过 1 个百分点时，预测振幅的中位捕捉率只有约 20%。

## 统计口径

统计局月度工业增加值的核心过程是：规模以上企业上报工业总产值，中类行业总产值乘以年度工业增加值率得到现价增加值，再使用分行业工业品价格指数缩减为可比价增加值并汇总。官方同比还会根据当期规模以上企业名录调整同口径基期。

公开数据无法在发布前完整复刻“中类行业总产值 × 增加值率 ÷ 分行业价格指数”，因此生产模型采用与它一致的统计桥：把同比中已经确定的固定价基数部分与当月生产冲击分开。

官方口径依据：

- [国家统计局：工业生产增长速度的统计方法](https://www.stats.gov.cn/zs/tjws/zytjzbqs/gysczzsd/202410/t20241025_1957170.html)
- [国家统计局：2026 年 1—2 月规模以上工业生产情况](https://www.stats.gov.cn/sj/zxfb/202603/t20260316_1962782.html)

## 预测结构

先用官方季调环比 `M002822189` 构造连锁固定价指数：

`I[t] = I[t-1] × (1 + MoM[t] / 100)`

预测月 `t` 当时，`I[t-1]` 和 `I[t-12]` 都已经发布，因此已知固定价承接项为：

`carry[t] = (I[t-1] / I[t-12] - 1) × 100`

原统计桥方程为：

`IAV_YoY[t] = 截距 + βcarry × carry[t] + Σ βk × HF[k,t]`

这个方程不包含上期工业增加值同比。上期信息只通过固定价指数中已经实现的产出路径进入，不再用上期同比作为预测锚。

在此基础上加入广发研报的固定行业扩散分支。该分支不再把 `carry` 当作一个可以被岭回归收缩的普通变量，而是先计算当月新增贡献：

`residual[t] = IAV_YoY[t] - carry[t]`

`report[t] = carry[t] + Ridge(行业水平、行业扩散宽度、行业加速度)`

最终预测为：

`forecast[t] = base[t] + w[t] × (report[t] - base[t])`

其中 `w[t]` 只用 `t` 月以前已经实现的严格样本外误差估计，并限制在 0—1；前 6 个回测月固定为 0.5。这样可以让行业扩散在有效时提高响应速度，在高频噪声较大时自动降低影响。

## 固定高频因子

| 因子 | 生产模块 | 变换 | 入模逻辑 |
|---|---|---|---|
| 六大发电集团日均耗煤 | 电力/能源 | 月均值同比 | 代理火电负荷和高耗能工业用能 |
| 247 家高炉开工率 | 黑色 | 同比百分点变化 | 代理钢铁生产强度 |
| 甲醇开工率 | 化工 | 同比百分点变化 | 代理连续生产化工负荷 |
| 全钢胎开工率 | 汽车/物流 | 同比百分点变化 | 代理商用车和轮胎配套生产 |
| 石油沥青装置开工率 | 基建需求 | 同比百分点变化 | 代理道路施工和地产链实物需求 |

因子名单每个月不变，不做动态相关性换因子。官方当月发电量、分行业工业增加值及工业产品产量与目标基本同时发布，只用于事后结构诊断，不进入发布前预测。

## 广发研报复现与固定 19 因子分支

研报《工业增加值如何预测？》的可复现核心是“同比方向扩散指数 + ARDL，MIDAS 辅助”。研报的 ARDL/MIDAS 高拟合度主要属于样本内结果，真正样本外展示仅覆盖 2023 年 7—8 月，因此本项目没有直接采用其样本内系数，而是把它的固定行业覆盖和扩散逻辑嵌入严格逐月走步框架。

固定 19 项代理指标为：六大发电集团耗煤、高炉开工、螺纹钢厂开工、粗钢、建筑钢材、PTA、甲醇、聚酯、乘用车批发、乘用车零售、广义乘用车产量、挖掘机销量、全钢胎、半钢胎、30 城商品房成交、螺纹钢表观消费、沥青开工、钢材库存、乘用车零售同比。

这些指标固定归入五个通道：

| 通道 | 统计含义 | 三类入模信息 |
|---|---|---|
| 能源 | 总体工业负荷 | 同比水平、同比是否继续加速、标准化加速度 |
| 黑色 | 钢铁生产与库存 | 同上 |
| 化工 | 连续生产行业负荷 | 同上 |
| 汽车 | 汽车产销与轮胎配套 | 同上 |
| 地产基建 | 工业品需求牵引 | 同上 |

行业内先等权聚合，行业间由岭回归估计贡献。这里没有机械照搬研报图表中的投入产出权重：一方面本地代理指标与原研报数据源并非完全一致，另一方面直接使用全样本权重会削弱走步回测的可审计性。

## 二月特殊处理

统计局 1 月不单独公布规模以上工业增加值，2 月公布 1—2 月累计同比。因此：

- 2 月的五个高频因子全部使用 1—2 月合并窗口，并与上年 1—2 月比较；
- 2 月不使用固定价承接项，避免把同批发布的 1 月环比信息泄漏进模型；
- 1 月不构造虚假单月工业增加值目标。

## 估计与防泄漏

- 原统计桥固定训练窗口：最近 36 个有效月；
- 广发残差分支固定训练窗口：最近 48 个有效月；
- 两个分支岭回归惩罚参数均为 32；
- 最少先验月数：18；
- 每个回测月只用该月以前的工业增加值目标估计系数；
- 承接项只使用预测月之前已发布的季调环比；
- 一致预期只在模型完成后合并评价，不参与因子、拟合或参数选择。

需要注意，季调环比历史值会修订。当前走步回测防止了“使用未来月份”的泄漏，但现有数据源未保存每次发布的历史版本，因此仍存在历史季调修订带来的小幅版本偏差。

## 本次严格走步结果

2023 年 3 月至 2026 年 7 月：

| 指标 | 原五因子模型 | 固定价统计桥 | 广发混合模型 |
|---|---:|---:|---:|
| RMSE | 0.989 | 0.840 | 0.813 |
| MAE | 0.767 | 0.644 | 0.605 |
| 预测值与实际值相关系数 | 0.350 | 0.634 | 0.720 |
| 月度变化相关系数 | 0.038 | 0.298 | 0.432 |
| 月度变化标准差/实际值 | 0.453 | 0.597 | 0.601 |
| 大幅变动振幅中位捕捉率 | 0.204 | 0.382 | 0.439 |
| 大幅变动 RMSE | 1.322 | 1.105 | 0.978 |

广发混合模型进一步改善了水平误差、相关性和大幅变动月份，但月度变化标准差仍只有实际值的约 60%。剩余差距主要来自现有高频数据对电子、电气机械、设备制造等现代制造业覆盖不足，以及公开数据无法事前完全还原同口径企业名录和分行业价格缩减。

共同样本中，广发混合模型与一致预期的 RMSE 分别为 0.796 和 1.120，模型绝对误差胜率为 56.8%。2026 年 8 月最新预测为 4.656%，原统计桥为 4.864%，已知固定价承接项为 4.320%，广发残差分支的在线权重为 0.252。

## 运行

```powershell
..\runtime\python\python.exe scripts\industrial_value_forecast_model.py
..\runtime\python\python.exe -m unittest discover -s tests -p test_industrial_value_forecast_model.py
```

主要产物：

- `data/industrial-value-model/forecast_results.json`：最新预测、走步历史、波动指标和一致预期对比；
- `scripts/industrial_value_forecast_model.py`：生产模型；
- `scripts/research_industrial_bottom_logic.py`：直接同比、模块桥和固定价方案的审计研究脚本；
- `scripts/research_industrial_gf_hybrid.py`：广发行业扩散残差桥及在线组合的研究复现脚本；
- `outputs/industrial-value-report-11-ocr.txt`：用户提供研报扫描件的 OCR 文本。

## 2026-08-28 修正版：固定单指标新息挑战层

本节结果替代上文较早版本的回测数字。原有行业汇总层会先把 19 个固定代理指标压缩为能源、黑色、化工、汽车和地产基建五个均值。这样做虽然稳健，但行业内部若同时出现强弱分化，正负新息会相互抵消。

修正版保留原有固定因子名单和固定价格承接项，并新增第二层挑战模型：

1. 每个固定代理指标分别转换为同比水平信号；开工率使用同比百分点变化，数量指标使用同比增速，已是同比的指标保留原值。
2. 每个指标只使用预测月之前的 36 个月历史做标准化，并截尾在正负 3 个标准差。
3. 用最近 24 个可用月份估计“实际工业增加值同比 - 固定价格承接项”，岭惩罚固定为 512，以控制小样本下的系数波动。
4. 挑战模型至少积累 24 个先验训练样本后才启用。启用后的权重只根据预测月之前已经实现的严格样本外误差估计，并限制在 0 到 1。
5. 一致预期仍只在全部预测完成后用于比较，不进入因子、回归、权重或参数选择。

生产方程变为：

`family[t] = base[t] + w1[t] * (family_residual[t] - base[t])`

`uncalibrated[t] = family[t] + w2[t] * (individual_residual[t] - family[t])`

`forecast[t] = uncalibrated[t] + prior_error_calibration[t]`

其中 `w1[t]`、`w2[t]` 和误差校准均只使用 `t` 月之前的严格样本外误差。模型仍不使用上期工业增加值同比作为预测锚。

2023 年 3 月至 2026 年 7 月严格走步结果：

| 指标 | 修正前生产模型 | 固定单指标新息模型 |
|---|---:|---:|
| RMSE | 0.692 | 0.663 |
| MAE | 0.575 | 0.581 |
| 预测值与实际值相关系数 | 0.732 | 0.756 |
| 方向命中率 | 68.42% | 71.05% |
| 转折点命中率 | 73.91% | 82.61% |
| 月度变化相关系数 | 0.481 | 0.552 |
| 月度变化标准差 / 实际值 | 0.595 | 0.633 |
| 大幅变动振幅中位捕捉率 | 0.394 | 0.500 |

2024 年 7 月至 2026 年 7 月的共同可用样本中，RMSE 从 0.725 降至 0.672；独立检查的 2025 年以后样本中，从 0.751 降至 0.728。新模型改善了转折和波动响应，但 MAE 小幅上升，说明它不是对所有普通月份都更优。当前 2026 年 8 月实时预测为 4.522%，固定价格承接项为 4.320%，单指标挑战权重为 1.000。

仍需注意：公开数据缺少发布时间早于工业增加值的分行业用电量、企业生产计划和现代制造业实物产出。官方发电量和分行业工业增加值与目标同批发布，只能做事后核验，不能作为当月预测因子。
