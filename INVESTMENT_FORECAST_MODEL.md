# 固定资产投资月频预测模型

## 结论

- 预测目标：国家统计局“固定资产投资（不含农户）完成额：累计同比”，iFinD EDB `M001620575`。
- 2026 年 8 月模型预测为 **-6.92%**，建议报数 **-6.9%**；基于样本外 RMSE 的约 68% 区间为 **[-7.92%, -5.92%]**。
- 截至 2026-08-24，一致预期 `M005682259` 尚无 8 月有效观测，所以当前月不计算点对点差值；历史共同样本比较不受影响。
- 2023-03 至 2026-07 的 38 个共同样本中，模型 RMSE 为 **1.003**，一致预期为 **1.589**，模型降低 **36.88%**；模型 MAE 为 **0.702**，一致预期为 **0.968**，降低 **27.44%**，逐月绝对误差胜率为 **60.53%**。
- 2025-02 至 2026-07 的 17 个结构下行样本中，模型 RMSE 为 **1.178**，一致预期为 **2.319**；模型平均偏差为 **+0.396** 个百分点。
- 修正后模型预测与当期真实值的相关性为 **0.946**，与前一期真实值的相关性为 **0.861**；旧累计同比锚模型对应数值为 **0.919** 和 **0.996**。

## 定档信息

- 生产版本：`investment-level-flow-bridge-v4-bias-corrected`。
- 定档日期：`2026-08-27`。
- 冻结范围：参数、候选桥、校准窗口和信息截点；后续自动刷新只允许加入新发布的观测值，不追改历史实时预测。
- 上线门槛：共同样本 RMSE 必须低于一致预期；一致预期始终物理隔离，只用于事后比较。

## 模型结构：固定额先行，同比后算

1. 可比固定额链：原始累计金额 `M001620537` 会受统计范围调整影响，不能直接跨年相除。模型用每个历史月份的官方同比连接成一条固定可比金额链，确保历史上由固定额反算的同比与官方值完全一致。
2. 普通月份增量：先预测当月固定投资增量，再加到上月已公布的可比累计固定额。候选包括去年同月固定增量、近期普通月份增量增速、高频施工桥和月频模型桥。
3. 高频施工桥：高炉、螺纹钢、动力煤、PTA、甲醇、汽车批零、沥青、水泥、商品房成交、螺纹库存等数据。每个历史月份统一只取当月 24 日及以前的观测，匹配当前实时信息集。
4. 月频模型桥：使用项目中工业增加值、新增人民币贷款和社会融资规模的历史实时预测；不使用一致预期，也不以事后真实值替换历史预测。
5. 分项固定额桥：基建和房地产累计金额先转成月度固定增量，其同比变化滞后一期进入模型。
6. 桥接基准校准：桥接回归窗口由48个月缩短为24个月；高频桥和月频桥分别使用此前9个普通月份的固定增量残差进行50%收缩校准。在线权重在RMSE之外额外惩罚持续同向偏差。
7. 1—2 月特殊处理：2 月发布的是1—2月合并累计额。独立期间额基准由上年同期间额状态87.5%与最近3个普通月固定增量状态12.5%组合，并在普通月训练和评分中排除2月。
8. 同比换算：预测可比累计固定额除以上年同月可比固定额，最后一步才得到累计同比。

上一期累计同比不进入金额增量方程、不作为预测锚，也不出现在候选权重计算中。

## 防泄漏规则

- 一致预期 `M005682259` 物理隔离，只在全部固定额预测与同比换算完成后合并评估。
- 预测月份 `t` 的回归训练目标和误差权重只使用 `< t` 的历史信息，采用 walk-forward 方式逐月重估。
- 同批发布的基建和房地产固定额只允许滞后一期。
- 高频数据在所有历史月采用与当前月相同的日历截点（24 日），不使用月底以后数据。
- 2 月合并期间额单独处理，不虚构不存在的 1 月官方值。

## 当前月固定额桥

- 2026 年 8 月预测月度可比固定增量：**5.872 万亿元指数额**。
- 加总后的可比累计固定额：**48.896 万亿元指数额**。
- 上年同月可比固定基数：**52.532 万亿元指数额**。
- 换算累计同比：`48.896 / 52.532 - 1 = -6.92%`。
- 当前在线权重：去年同月固定增量 14.54%，近期增量增速 31.06%，高频桥 26.19%，月频模型桥 28.21%。两条桥的输入已经过各自的实时残差收缩校准。

这里的“指数额”是为保持官方可比口径而递推出的固定金额尺度，不能与国家统计局原始累计金额直接混用。

## 运行

```powershell
$env:IFIND_INSECURE_TLS='1'
wsl.exe python3 '/mnt/c/Users/wangzhiwei202307/Documents/宏观网页面板/source-main/scripts/fetch_investment_forecast_data.py' --call-py '/mnt/c/Users/wangzhiwei202307/Documents/固收投委会/skills/ifind-finance-data/call.py' --resume
..\runtime\python\python.exe scripts\investment_level_forecast_model.py
..\runtime\python\python.exe -m unittest discover -s tests -p test_investment_forecast_model.py
```

主要产物：

- `data/investment-model/source_data.json`：固定 ID 的目标累计额、目标同比、分项固定额与物理隔离的一致预期。
- `data/investment-model/forecast_results.json`：当前固定额预测、同比换算、走步历史、滞后诊断及共同样本比较。
- `outputs/investment-model/investment_forecast_timeseries.csv`：作图与审计用逐月序列。
