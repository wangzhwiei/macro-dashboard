# M2、新增人民币贷款与社融预测模型

模型面向 2026 年 8 月及后续月度更新。第五版按各指标的数据生成过程分别建模，重建“社融发布前信息集”，并坚持严格的逐月样本外检验。

## 生产冻结版本

- 版本：`credit-v1.0.0`
- 冻结日期：2026-08-25
- 冻结范围：因子集合、候选模型、窗口、正则化参数、候选选择规则和残差校正规则。
- 允许更新：新公布的目标值、月频宏观值、社融分项和预测时点已经发生的票据利率数据。
- 禁止更新：不得因为后续回测结果自动修改参数或回写历史预测；模型升级必须启用新的版本号并单独验证。

## 模型结构

- M2 同比：不以上月同比为预测底座。模型使用人民银行 M2 期末余额（iFinD `M001625221`），先预测当月余额增量，再叠加到上月余额，最后除以上年同月余额得到同比。四个预先限定的稀疏候选最多使用六个变量：过去 12/24/36 个月的同月余额增量、最近三个月平均余额增量、无一致预期的贷款模型预测、3 个月国股票据转贴利率同比变化。社融模型预测已从 M2 特征中删除。每个月只根据此前 48 个月的样本外误差选取候选，训练样本不足时使用历史余额增量的季节组合。
- 新增人民币贷款：以去年同月为基准，加上过去 12 个月相对更早 12 个月信用水位变化的 25%。该系数冻结自 2010—2019 年的预验证。2024 年起再使用当月 3 个月国股票据转贴利率的同比变化修正残差；斜率只使用此前已公布的贷款结果估计，并限制在 ±0.5。最终再施加小幅同月/近期残差校正，所有残差均严格滞后一期。
- 社融：运行三个挑战模型，再按此前 36 个月的样本外 RMSE 逆方差加权：
  1. 三年同月值、过去一年信用水位及其年度变化的滚动 Ridge；
  2. 十个央行社融分项与“其他/对账项”的分项加总模型。
  3. 使用社融发布前信息集的月频宏观 Ridge；历史回测直接使用当月已公布 CPI、PPI、PMI，工业增加值滞后一个月。

  组合后向最近三年同月实际值的中位数收缩 25%，并根据此前月份误差做小幅稳健校正。宏观候选只有在此前样本外误差支持时才获得权重。

## 防泄漏规则

一致预期只在全部预测与权重确定后用于比较，不进入特征、训练、调参、候选选择或加权。历史回测每个月只使用当时可观测数据：目标值及模型残差必须来自此前月份；票据利率允许使用预测月内已发生的市场数据。由于 CPI、PPI、PMI 通常在社融前公布，历史回测使用当月真实发布值；工业增加值在社融后公布，因此只使用上月值。当前月则按数据抓取时点判断：尚未发布的变量使用项目自身的无一致预期预测值。社融分项和市场代理全部校验固定 iFinD provider ID。

输出中的 `performanceGateVsConsensus` 机械比较同样本 RMSE。只有模型 RMSE 严格低于一致预期才会标记 `passed: true`；模型不会通过缩短样本或改变比较口径宣称胜出。

## 运行

```powershell
..\runtime\python\python.exe scripts\fetch_credit_forecast_data.py --resume
..\runtime\python\python.exe scripts\credit_forecast_model.py
..\runtime\python\python.exe -m unittest discover -s tests -p test_credit_forecast_model.py
```

输出位于 `data/credit-model/forecast_results.json`，包含当前预测、区间、样本外诊断、与一致预期的同样本比较以及可直接绘图的历史序列。
