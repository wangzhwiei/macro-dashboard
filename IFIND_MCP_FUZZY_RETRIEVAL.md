# iFinD MCP 模糊权限提数规则

依据 iFinD 官方 `ifind-finance-data` 1.4.0 技能说明及当前密钥的实时 `tools/list` 结果整理。

## 当前权限边界

- 官方完整 EDB 设计包含 `search_edb`（指标搜索）和 `get_edb_data`（指标取数）。
- 当前密钥实时只开放 `get_edb_data(query)`。
- 因而当前调用不是固定 ID API；`get_edb_data` 内部先对自然语言字段做模糊匹配，再返回最接近的指标数据。
- `M005682256` 等 provider ID 只能用于返回后验真，不能直接作为当前 query 的可靠取数键。

## 正确查询顺序

1. 每个请求只放一个指标，避免多指标语义竞争。
2. query 只包含核心指标语义，不拼接时间范围。已验证的正确形式是 `出口金额:预测平均值:当月同比`。
3. 当前模糊召回会自行返回可用历史窗口；把 `2015-01至2026-07` 等时间文本拼进指标名会导致 `datas=[]`。
4. 若失败，依次尝试少量同义表达；不要堆叠国家、来源、排除条件、固定 ID 或日期。
5. 所有请求顺序执行；不确定权益时按免费版并发上限每秒 2 个控制。

## 返回后强校验

查询字符串只负责召回，以下字段只用于验真：

- `attrs.<series>.index_id` 必须等于预期 provider ID；
- 返回列名必须与目标指标完全一致，或属于事先批准的正式别名；
- `freq=M`、`unit=%`、`country=中国`；
- 一致预期来源必须为 `同花顺金融`；实际值来源必须为 `海关总署`；
- 数据日期、行数和数值范围必须合理。
- 同一概念可能同时召回“总额”和“总值”等正式序列；若 provider ID 不同，需用重叠期数据、更新时间和业务口径交叉核对，不因名称近似直接合并。

任何一项不符都拒绝，不允许因为“走势看起来合理”而使用。

## 进出口目标

- 出口一致预期：`M005682256`，`预测平均值:出口金额(美元计价):当月同比`。
- 进口一致预期：`M005682257`，`预测平均值:进口金额(美元计价):当月同比`。
- 已验证出口实际值：`M002888330`，`出口总额(美元计价):当月同比`。
- 已验证进口实际值：`M002888203`，`进口总额(美元计价):当月同比`。

对应的无日期查询词：

- `出口金额:预测平均值:当月同比`
- `进口金额:预测平均值:当月同比`
- `出口总额(美元计价):当月同比`
- `进口总额(美元计价):当月同比`

## 与先前做法的区别

- 不再直接把 provider ID 传给 `get_edb_data`；该方式曾错误返回 M0 同比。
- 不再把 ID、来源、国家、频率、单位和排除条件全部写入一个长 query；复杂度会降低召回率。
- 不再在 query 中附加时间范围；本次实测附加时间会让两条一致预期均空返回。
- 不把 MCP 返回的第一条结果视为正确答案；先解析 `columns` 和 `attrs` 再决定是否接受。
